name: Build Me CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]
    
env:
  KW_URL: http://localhost:8080
  KW_PROJECT: cxx_test_private

jobs:
  build:

    runs-on: self-hosted

    steps:
    - uses: actions/checkout@v3
    - name: make with Klocwork
      run: kwinject make
    - name: analyze with Klocwork
      run: kwbuildproject --url $Env:KW_URL/$Env:KW_PROJECT -o kwtables -f kwinject.out
    - name: load Klocwork results
      run: kwadmin --url $Env:KW_URL load $Env:KW_PROJECT kwtables
